"""
Extract module for PDF data processing.
This module contains functions to extract text and metadata from PDF files.
It uses PyMuPDF (fitz) for PDF handling.
It also includes error handling for corrupted or unreadable PDFs.
and store it in  json format
for testing data is taken from /backend/app/public/sample-pdfs/Resume.pdf (add right path)
"""
